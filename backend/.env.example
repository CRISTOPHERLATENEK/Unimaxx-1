# Ambiente
NODE_ENV=development

# Porta do servidor (a Hostinger normalmente injeta automaticamente; deixe como padrão)
PORT=3001

# JWT Secret (obrigatório em produção)
JWT_SECRET=troque-esta-chave-em-producao

# CORS Origins (separados por vírgula)
# Em produção: https://seudominio.com,https://www.seudominio.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Caminho do banco SQLite (recomendado apontar para um local persistente no Hostinger)
# Ex.: DB_PATH=/home/SEU_USUARIO/storage/database.sqlite
DB_PATH=./database.sqlite

# Pasta de uploads (recomendado apontar para um local persistente no Hostinger)
# Ex.: UPLOAD_DIR=/home/SEU_USUARIO/storage/uploads
UPLOAD_DIR=./uploads

# Seed do admin
# - Em DEV: o sistema cria admin automaticamente
# - Em PROD: defina SEED_ADMIN=true para criar (uma vez) e depois desative
SEED_ADMIN=true
ADMIN_EMAIL=admin@linx.com
ADMIN_PASSWORD=admin123
ADMIN_NAME=Administrador
